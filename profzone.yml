name: service-id
group: eden
owner: profzone
version: 0.0.0
description: ID生成服务
program_language: golang
workflow:
  extends: feature-pr
scripts:
  build: go build -v -o $PROJECT_NAME && libtools swagger
  test: go test
